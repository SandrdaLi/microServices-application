apply plugin: "war"

bootWar {
    mainClassName = "com.prudential.pgim.fi.MicroServicesApplicationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
